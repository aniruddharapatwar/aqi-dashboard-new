# AQI Dashboard - Python Dependencies
# UPDATED FOR LSTM MODELS - Added TensorFlow/Keras
# Last updated: 2025-10-30

# Core Framework (no numpy dependencies)
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Data Processing - CRITICAL: Both must support numpy 2.x
numpy>=2.0.0,<3.0.0
pandas>=2.2.0,<3.0.0          # pandas 2.2.0+ supports numpy 2.x

# Machine Learning - VERIFIED for numpy 2.x compatibility
scikit-learn>=1.5.0,<2.0.0    # scikit-learn 1.5.0+ has full numpy 2.x support
xgboost>=2.1.0,<3.0.0         # xgboost 2.1.0+ supports numpy 2.x (keeping for legacy)

# Deep Learning for LSTM Models - NEW
tensorflow>=2.15.0,<3.0.0     # For GPU-enabled systems
# OR use tensorflow-cpu for production without GPU:
# tensorflow-cpu>=2.15.0,<3.0.0

keras>=3.0.0                  # High-level API for TensorFlow

# Supporting ML packages
joblib>=1.3.2                  # Used by scikit-learn, numpy-compatible
scipy>=1.13.0                  # scipy 1.13.0+ supports numpy 2.x (scikit-learn dependency)

# AI Assistant (no numpy dependencies)
google-generativeai>=0.8.0

# Additional Dependencies (no numpy conflicts)
pydantic>=2.0.0,<3.0.0
python-dotenv>=1.0.0
aiofiles>=23.2.0

# Optional: Include for better error messages
setuptools>=65.0.0

# NOTE: If deploying on Railway/Render/Heroku without GPU, use tensorflow-cpu:
# Comment out 'tensorflow>=2.15.0' above and uncomment the line below:
# tensorflow-cpu>=2.15.0,<3.0.0